// src/services/aiService.ts
export async function getAIItinerary(destination: string, dates: string): Promise<string> {
    // Aquí va la lógica para obtener el itinerario de la IA
    // Por ejemplo, llamar a una API o simular un resultado
    return `Itinerario para ${destination} en las fechas ${dates}`;
  }
  